
╔══════════════════════════════════════════════════════════════════════════════════════╗
║                    程式碼重構 - 修改前 vs 修改後對比圖                                 ║
╚══════════════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────────────┐
│                                  修改前架構                                           │
└──────────────────────────────────────────────────────────────────────────────────────┘

    serial_worker.py (274 行)          adb_worker.py (213 行)         ssh_worker.py (455 行)
    ┌─────────────────────┐           ┌─────────────────────┐        ┌─────────────────────┐
    │ • 序列埠連線        │           │ • ADB 連線檢查      │        │ • SSH 連線管理      │
    │ • 指令執行          │           │ • 指令執行          │        │ • 指令執行          │
    │ • DELAY 處理 ✗      │           │ • DELAY 處理 ✗      │        │ • DELAY 處理 ✗      │
    │ • SHOW 處理 ✗       │           │ • SHOW 處理 ✗       │        │ • SHOW 處理 ✗       │
    │ • 進度管理 ✗        │           │ • 進度管理 ✗        │        │ • 進度管理 ✗        │
    │ • 錯誤處理 ✗        │           │ • 錯誤處理 ✗        │        │ • 錯誤處理 ✗        │
    │ • 回調管理 ✗        │           │ • 回調管理 ✗        │        │ • 回調管理 ✗        │
    └─────────────────────┘           └─────────────────────┘        └─────────────────────┘
           ✗ = 重複程式碼                   ✗ = 重複程式碼                ✗ = 重複程式碼

    問題：
    ❌ 70% 程式碼重複
    ❌ 修改 DELAY 需要改 3 個檔案
    ❌ 容易遺漏，產生 Bug
    ❌ 測試成本高
    ❌ 維護困難


┌──────────────────────────────────────────────────────────────────────────────────────┐
│                                  修改後架構                                           │
└──────────────────────────────────────────────────────────────────────────────────────┘

                            BaseWorker (基礎類別)
                    ┌───────────────────────────────────────┐
                    │ • DELAY 處理 ✓ (統一實現)            │
                    │ • SHOW 處理 ✓ (統一實現)             │
                    │ • 進度管理 ✓ (統一實現)              │
                    │ • 錯誤處理 ✓ (統一實現)              │
                    │ • 回調管理 ✓ (統一實現)              │
                    │ • 執行流程 ✓ (模板方法)              │
                    └───────────────────────────────────────┘
                                     │
                    ┌────────────────┼────────────────┐
                    │                │                │
                    ▼                ▼                ▼
        SerialWorker (~100 行)  ADBWorker (~80 行)  SSHWorker (~120 行)
        ┌─────────────────┐    ┌─────────────────┐  ┌─────────────────┐
        │ • 序列埠連線    │    │ • ADB 連線檢查  │  │ • SSH 連線管理  │
        │ • 序列埠執行    │    │ • ADB 執行      │  │ • SSH 執行      │
        │ • 序列埠斷線    │    │ • ADB 斷線      │  │ • SSH 斷線      │
        └─────────────────┘    └─────────────────┘  └─────────────────┘
        只實現序列埠邏輯       只實現 ADB 邏輯      只實現 SSH 邏輯

    優點：
    ✅ 0% 程式碼重複
    ✅ 修改 DELAY 只需改 1 個檔案
    ✅ 自動同步，不會遺漏
    ✅ 測試成本低
    ✅ 易於維護


┌──────────────────────────────────────────────────────────────────────────────────────┐
│                              修改 DELAY 指令的流程對比                                 │
└──────────────────────────────────────────────────────────────────────────────────────┘

修改前：
────────────────────────────────────────────────────────────────────────────────────
需求：DELAY 指令支援小數秒 (如 DELAY:1.5)

步驟 1: 修改 serial_worker.py
    ┌──────────────────────────────────────────────────────────────┐
    │ # serial_worker.py (第 105 行)                               │
    │ delay_match = re.match(r'^DELAY:(\d+)$', cmd)                │
    │                                      ↓ 改為                  │
    │ delay_match = re.match(r'^DELAY:(\d+\.?\d*)$', cmd)          │
    │ delay_seconds = int(delay_match.group(1))                    │
    │                 ↓ 改為                                       │
    │ delay_seconds = float(delay_match.group(1))                  │
    └──────────────────────────────────────────────────────────────┘

步驟 2: 修改 adb_worker.py (重複相同修改)
    ┌──────────────────────────────────────────────────────────────┐
    │ # adb_worker.py (第 135 行)                                  │
    │ delay_match = re.match(r'^DELAY:(\d+)$', cmd)                │
    │                                      ↓ 改為                  │
    │ delay_match = re.match(r'^DELAY:(\d+\.?\d*)$', cmd)          │
    │ delay_seconds = int(delay_match.group(1))                    │
    │                 ↓ 改為                                       │
    │ delay_seconds = float(delay_match.group(1))                  │
    └──────────────────────────────────────────────────────────────┘

步驟 3: 修改 ssh_worker.py (又是重複相同修改)
    ┌──────────────────────────────────────────────────────────────┐
    │ # ssh_worker.py (第 378 行)                                  │
    │ delay_match = re.match(r'^DELAY:(\d+)$', cmd)                │
    │                                      ↓ 改為                  │
    │ delay_match = re.match(r'^DELAY:(\d+\.?\d*)$', cmd)          │
    │ delay_seconds = int(delay_match.group(1))                    │
    │                 ↓ 改為                                       │
    │ delay_seconds = float(delay_match.group(1))                  │
    └──────────────────────────────────────────────────────────────┘

步驟 4: 測試三種模式
    • 測試 Console 模式 ✓
    • 測試 ADB 模式 ✓
    • 測試 SSH 模式 ✓

風險：
    ❌ 需要修改 3 個檔案 (6 處修改)
    ❌ 容易忘記修改某個檔案
    ❌ 容易修改不一致
    ❌ 測試成本高


修改後：
────────────────────────────────────────────────────────────────────────────────────
需求：DELAY 指令支援小數秒 (如 DELAY:1.5)

步驟 1: 修改 base_worker.py (只需修改一處)
    ┌──────────────────────────────────────────────────────────────┐
    │ # base_worker.py                                             │
    │ def _handle_delay_command(self, cmd: str) -> bool:           │
    │     delay_match = re.match(r'^DELAY:(\d+)$', cmd)            │
    │                                  ↓ 改為                      │
    │     delay_match = re.match(r'^DELAY:(\d+\.?\d*)$', cmd)      │
    │     delay_seconds = int(delay_match.group(1))                │
    │                     ↓ 改為                                   │
    │     delay_seconds = float(delay_match.group(1))              │
    └──────────────────────────────────────────────────────────────┘
                                     │
                    ┌────────────────┼────────────────┐
                    │                │                │
                    ▼                ▼                ▼
            SerialWorker        ADBWorker        SSHWorker
            自動同步 ✓          自動同步 ✓       自動同步 ✓

步驟 2: 測試三種模式 (自動同步，行為一致)
    • 測試 Console 模式 ✓
    • 測試 ADB 模式 ✓
    • 測試 SSH 模式 ✓

優點：
    ✅ 只需修改 1 個檔案 (2 處修改)
    ✅ 三個 worker 自動同步
    ✅ 保證行為一致
    ✅ 測試成本低


┌──────────────────────────────────────────────────────────────────────────────────────┐
│                                  數據對比                                             │
└──────────────────────────────────────────────────────────────────────────────────────┘

程式碼量：
────────────────────────────────────────────────────────────────────────────────────
                        修改前          修改後          改善
    serial_worker.py    274 行         ~100 行         -63%
    adb_worker.py       213 行         ~80 行          -62%
    ssh_worker.py       455 行         ~120 行         -74%
    base_worker.py      0 行           ~200 行         新增
    ────────────────────────────────────────────────────────────────────
    總計                942 行         ~500 行         -47%


重複程式碼：
────────────────────────────────────────────────────────────────────────────────────
                        修改前          修改後          改善
    DELAY 處理          3 份            1 份            -67%
    SHOW 處理           3 份            1 份            -67%
    進度管理            3 份            1 份            -67%
    錯誤處理            3 份            1 份            -67%
    ────────────────────────────────────────────────────────────────────
    平均重複率          70%             0%              -100%


修改成本：
────────────────────────────────────────────────────────────────────────────────────
                        修改前          修改後          改善
    修改檔案數          3 個            1 個            -67%
    修改行數            6-12 行         2-4 行          -67%
    測試模式數          3 個            3 個            相同
    遺漏風險            高              無              ↓↓↓


維護成本：
────────────────────────────────────────────────────────────────────────────────────
                        修改前          修改後          改善
    新增功能            困難            容易            ↑↑
    修改邏輯            困難            容易            ↑↑
    除錯時間            長              短              ↓↓
    學習曲線            平緩            稍陡            -


┌──────────────────────────────────────────────────────────────────────────────────────┐
│                                  建議                                                 │
└──────────────────────────────────────────────────────────────────────────────────────┘

立即可做 (不需重構)：
────────────────────────────────────────────────────────────────────────────────────
    1. 建立測試案例，確保重構不會破壞功能
    2. 建立程式碼審查檢查清單
    3. 記錄當前已知問題

短期目標 (1-2 週)：
────────────────────────────────────────────────────────────────────────────────────
    1. 實施 BaseWorker
    2. 重構 SerialWorker 作為範例
    3. 驗證效果

長期目標 (1-2 個月)：
────────────────────────────────────────────────────────────────────────────────────
    1. 完成所有 worker 重構
    2. 建立完整測試體系
    3. 更新文件


═══════════════════════════════════════════════════════════════════════════════════════
                                結論
═══════════════════════════════════════════════════════════════════════════════════════

    修改前：改 A 影響 B，需要手動同步，容易遺漏 ❌
    修改後：改 A 自動同步 B，不會遺漏，保證一致 ✅

    建議：實施重構，長期效益遠大於短期成本

═══════════════════════════════════════════════════════════════════════════════════════
